import { CustomEvent } from '../core/CustomEventEmitter';
export interface StartingEventInput {
    startingDate: string;
}
export declare class StartingEvent implements CustomEvent {
    name: string;
    startingDate: string;
    constructor({ startingDate }: StartingEventInput);
    get data(): {
        startingDate: string;
    };
}
