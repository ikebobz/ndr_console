import { CustomEvent } from '../core/CustomEventEmitter';
export interface CanceledEventInput {
    cancelDate: string;
}
export declare class CanceledEvent implements CustomEvent {
    name: string;
    cancelDate: string;
    constructor({ cancelDate }: CanceledEventInput);
    get data(): {
        cancelDate: string;
    };
}
