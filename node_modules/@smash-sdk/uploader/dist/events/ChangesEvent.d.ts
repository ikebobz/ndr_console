import { CustomEvent } from '../core/CustomEventEmitter';
import { UploaderEvent } from '../interface/Event';
export interface ChangesInput {
    event: UploaderEvent;
}
export declare class ChangesEvent implements CustomEvent {
    name: string;
    event: Partial<UploaderEvent>;
    constructor({ event }: ChangesInput);
    get data(): {
        event: Partial<UploaderEvent>;
    };
}
