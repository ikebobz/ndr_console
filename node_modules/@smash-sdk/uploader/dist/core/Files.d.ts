/// <reference types="node" />
import { FileItem } from './FileItem';
export declare class Files {
    files: Map<string, FileItem>;
    _size: number;
    constructor(files?: File[] | string[] | {
        name: string;
        file: File | string;
    }[] | {
        name: string;
        content: string | Buffer;
    }[]);
    get length(): number;
    get size(): number;
    add(file: FileItem): Files;
    get(name: string): FileItem;
    has(name: string): boolean;
    remove(name: string): FileItem;
    list(): FileItem[];
    forEach(callback: ((file: FileItem, index?: number, files?: Map<string, FileItem>) => void)): void;
}
