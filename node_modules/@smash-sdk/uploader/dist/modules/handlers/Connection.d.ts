import { CustomEventEmitter } from '../../core/CustomEventEmitter';
import { Task } from '../tasks/Task';
export declare class Connection extends CustomEventEmitter {
    private task;
    readonly name: string;
    constructor(name: string);
    isBusy(): boolean;
    isAvailable(): boolean;
    wakeUp(): Connection;
    available(): Connection;
    ping(): Connection;
    private busy;
    progress(loaded: string, part: any): void;
    private startTask;
    getTask(): Task | null;
    private endTask;
    execute(task: Task): Promise<Task>;
    reset(): void;
}
