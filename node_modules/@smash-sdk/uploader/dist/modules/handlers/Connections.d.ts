import { CustomEventEmitter } from '../../core/CustomEventEmitter';
import { Connection } from './Connection';
export declare class Connections extends CustomEventEmitter {
    private connections;
    updateConnectionNumber(number: number): Connection[];
    get length(): number;
    reset(): Connections;
    get(name: string): Connection;
    list(): Connection[];
    forEach(callback: ((connection: Connection, id?: string, connections?: Map<string, Connection>) => void)): void;
    private addConnection;
    private removeConnection;
    ping(): Connections;
}
