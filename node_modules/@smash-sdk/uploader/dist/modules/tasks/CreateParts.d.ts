import { SDKError } from '@smash-sdk/core';
import { Context } from '../../core/Context';
import { FileItem } from '../../core/FileItem';
import { Parts } from '../../core/Parts';
import { TaskError } from '../../errors/TaskError';
import { AbstractTask } from './AbstractTask';
import { Task } from './Task';
export declare class CreateParts extends AbstractTask<Task> {
    private transfer;
    private file;
    private partsToCreate;
    private createTransferFilePartsParameters;
    private response;
    protected readonly sdkFatalErrors: (typeof SDKError)[];
    constructor(context: Context, file: FileItem, partsToCreate: Parts);
    preProcess(context: Context): Task | null;
    process(): Promise<CreateParts>;
    postProcess(context: Context): Task | null;
    processError(): TaskError;
}
