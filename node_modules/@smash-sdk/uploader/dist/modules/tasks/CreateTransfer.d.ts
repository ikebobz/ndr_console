import { SDKError } from '@smash-sdk/core';
import { Context } from '../../core/Context';
import { TaskError } from '../../errors/TaskError';
import { UploaderError } from '../../errors/UploaderError';
import { AbstractTask } from './AbstractTask';
import { Task } from './Task';
export declare class CreateTransfer extends AbstractTask<Task> {
    private transfer;
    private createTransferParameters;
    private createTeamTransferParameters;
    private createDropboxTransferParameters;
    private response;
    protected readonly sdkFatalErrors: typeof SDKError[];
    protected readonly uploaderFatalErrors: typeof UploaderError[];
    constructor(context: Context);
    isFirstTask(): boolean;
    preProcess(context: Context): Task | null;
    process(): Promise<CreateTransfer>;
    postProcess(context: Context): Task | null;
    processError(): TaskError;
}
