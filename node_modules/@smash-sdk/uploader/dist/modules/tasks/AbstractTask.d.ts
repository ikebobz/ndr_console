import { SDKError } from '@smash-sdk/core';
import { Context } from '../../core/Context';
import { ContextContainer } from '../../core/ContextContainer';
import { TaskError } from '../../errors/TaskError';
export declare abstract class AbstractTask<T> extends ContextContainer {
    executionNumber: number;
    maxExecutionNumber: number;
    delay: number;
    protected readonly sdkFatalErrors: typeof SDKError[];
    error?: TaskError | null;
    constructor(context: Context);
    isFirstTask(): boolean;
    isLastTask(): boolean;
    abstract preProcess(context: Context): T | null;
    execute(): Promise<T>;
    abstract process(): Promise<T>;
    abstract postProcess(context: Context): T | null;
    isOnError(): boolean;
    protected clearError(): AbstractTask<T>;
    processError(): TaskError;
    setDelay(delay: number): AbstractTask<T>;
}
