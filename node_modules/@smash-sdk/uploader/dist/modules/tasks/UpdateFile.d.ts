import { SDKError } from '@smash-sdk/core/dist';
import { Context } from '../../core/Context';
import { FileItem } from '../../core/FileItem';
import { TaskError } from '../../errors/TaskError';
import { AbstractTask } from './AbstractTask';
import { Task } from './Task';
export declare class UpdateFile extends AbstractTask<Task> {
    private transfer;
    private file;
    private updateTransferFileParameters;
    private response;
    protected readonly sdkFatalErrors: typeof SDKError[];
    constructor(context: Context, file: FileItem);
    preProcess(context: Context): Task | null;
    private updateFile;
    process(): Promise<UpdateFile>;
    processError(): TaskError;
    postProcess(context: Context): Task | null;
    private retryFileParts;
}
